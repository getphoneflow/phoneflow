{
  "name": "phoneflow",
  "private": true,
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "lint": "biome lint .",
    "format": "biome check --write --linter-enabled=false ."
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "typescript": "catalog:",
    "zod": "catalog:"
  }
}
